import * as ngs from '../src' // normally you'd have this in another package and
                         // import ngs from 'your-package' or '@ngs/lib'

// then you use like this:

ngs.num.isWorldAnswer(57)
